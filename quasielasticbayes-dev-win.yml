name: quasielasticbayes-dev

channels:
  - conda-forge

dependencies:
  - python =3.8.*
  - numpy =1.16.*
  - scipy
  - pytest
  - pre-commit >=2.15
  - pip:
    - cython >=0.29.32 # stops conda getting the wrong version
