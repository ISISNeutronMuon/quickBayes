name: quasielasticbayes-dev

channels:
  - conda-forge

dependencies:
  - gfortran_osx-64
  - python=3.6.*
  - numpy=1.16.*
  - pytest
  - pre-commit >=2.15
  - clang
  - aws-sdk-cpp
  - pip:
     - cython>=0.29.32 # needed due to conda getting the wrong version
